subdir('core')
subdir('gui')

source_file = files(['main.cpp',])

executable('boolean_operator',
           sources: source_file,
           dependencies: [boolean_operator_gui_dep],
           include_directories: includeDir
          )
